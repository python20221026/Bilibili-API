{
  "name": "bilibili-api",
  "version": "1.0.0",
  "description": "Bilibili parsing API as a RandallFlare (Cloudflare workerd) worker — wbi-signed, cookie in env, meting-style HMAC auth, R2 reverse-proxy cache, guest mode.",
  "main": "src/worker.js",
  "type": "module",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "lint": "oxlint .",
    "test": "node test/parity.mjs"
  },
  "dependencies": {
    "esbuild": "^0.24.2"
  },
  "devDependencies": {
    "oxlint": "^1.2.0"
  }
}
